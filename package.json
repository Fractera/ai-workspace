{
  "name": "fractera-light",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted AI development workspace",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix app\" \"npm run dev --prefix bridges/platforms\"",
    "start": "concurrently \"npm run start --prefix app\" \"npm run start --prefix bridges/platforms\"",
    "install:all": "npm install --prefix app && npm install --prefix bridges/platforms"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
