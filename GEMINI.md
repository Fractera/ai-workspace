# Gemini CLI — Agent Config

⚠️ **STOP. Read `AGENT.md` first.**
All architecture rules, forbidden zones, workflow, component limits,
skills-first policy and response style are defined there — not here.

---

## Gemini CLI specific

**CLI auth:** `gemini auth` (Google account subscription, no API key needed)
**Bridge port:** `:3203`
**Non-interactive flag:** `--prompt` + `--output-format stream-json` + `--yolo`
**Models:** Gemini 2.5 Pro, Gemini 2.5 Flash, Gemini 2.0 Flash
